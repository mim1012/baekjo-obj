import { QnA } from '@/types';

// 운영 전 가짜 문의(김민수·이지은 등)를 제거 — DB(qna_config)에 행이 없을 때 이 폴백이 그대로
// 공개 화면에 노출되던 문제(2026-07-27)를 없애기 위해 빈 배열로 둔다. 실제 상품문의는 관리자가
// 등록하거나 사용자 문의가 DB에 쌓이며 채워진다.
export const qnaList: QnA[] = [];
