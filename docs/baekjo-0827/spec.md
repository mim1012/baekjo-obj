# 백조오브제 2026-08-27 고객 수정 반영 명세

## 목적

고객 원본 폴더 `C:\Users\PC_1M\Desktop\0827 백조오브제 수정본`의 89개 파일을 기준으로 확인된 요청을 작업 ID 단위로 구현·검증한다. 이 문서가 요구사항의 정본이며, 원본 파일 커버리지는 `source-matrix.md`와 `source-index.md`를 함께 본다.

## 상태 정의

- `source-confirmed`: 고객 원본에서 요청 또는 참고자료 존재를 확인함
- `source-duplicate`: 동일 해시 중복본이며 대표 원본과 같은 요구로 취급함
- `visual-review-needed`: HWPX/PDF/PPTX/이미지/영상의 화면형 근거를 아직 Preview로 직접 판독하지 않음
- `implementation-pending`: 아직 제품 코드 반영 여부를 검증하지 않음
- `implementation-partial`: 구조 또는 일부 문구만 반영됨
- `implementation-verified`: 고객 원본, 자동 검증, 로컬 화면 확인이 모두 기록됨
- `decision-needed`: 구현 전에 고객 또는 담당자 결정이 필요함

## 구현 순서

헤더/푸터 → 홈 → 셀렉션 → 케어 → 브랜드 공통/브랜드별 → B2B/펫보험 → 관리자 제출자료 → 통합 검증

## 공통 / 헤더 / 푸터

- `HEADER-01` 헤더 메뉴를 `셀렉션 / 브랜드 / 케어 / 펫보험 / 백조 오브제 / B2B`로 수정한다.
- `HEADER-02` `백조 오브제` 하위 메뉴는 `검증 기준 / 전문가 칼럼 / 보호자 후기 / 소식`으로 구성한다.
- `FOOTER-01` 푸터는 로고, 1:1 문의, 이용약관, 개인정보처리방침, 사업자정보, SNS `@BAEKJO OBJET` 묶음으로 간결하게 유지한다.
- `FOOTER-02` 로고 아래 `PET LIFE CURATION` 문구는 삭제한다.

## 홈

- `HOME-01` 메인 히어로: 상단 `Curated Pet Brands`, 제목 `좋은 브랜드를 찾고 계셨나요?`, 본문 `좋은 브랜드는 결과입니다. 백조 오브제는 그 과정까지 확인합니다.`, 기존 버튼 문구 유지, `백조오브제 Audit을 통과한 브랜드만 소개합니다.` 노출.
- `HOME-02` `우리 아이를 위한 3가지 솔루션`: 앞뒤 섹션과 겹치므로 삭제하거나 메인 첫 화면 바로 아래 3개 진입 카드로 이동. 최종 방식은 결정 필요.
- `HOME-03` 브랜드 슬라이드: 브랜드 로고, 한 줄 설명, `브랜드관 보기` 버튼만 유지하고 `01 / 08` 번호 표시는 삭제한다.
- `HOME-04` Audit: 현재보다 상단 배치, 섹션 비중 확대, 좌측 강한 제목과 짧은 설명, 우측 Audit 엠블럼 또는 실제 Audit 문서 이미지, 하단 4개 기준 `브랜드 철학 / 성분·원료 / 제조 과정 / 사용 경험`.
- `HOME-05` 메인 추천상품: 최대 3개만 노출한다.
- `HOME-06` 보험 영역: 본문 `같은 품종이라도, 나이와 기왕력에 따라 우리 아이에게 맞는 보험은 달라집니다.`, 버튼 `보험 분석 시작하기`.
- `HOME-07` `6 (1).hwpx` 요청: 좌측 `보호자 후기`, 우측 `소식` 영역을 확인·반영한다.

## 셀렉션 / 상품

- `SHOP-00` 셀렉션 히어로: `BAEKJO OBJET SELECTION`, 제목 `우리 아이를 위한 좋은 선택`, 본문 `백조오브제의 기준으로 살펴보고 선택한 제품을 소개합니다.`
- `SHOP-01` 기본 필터는 `반려동물 / 카테고리 / 브랜드 / 가격` 4개만 노출한다.
- `SHOP-02` 상세 필터에는 `고민 / 평점`을 유지하고 `연령` 항목은 삭제한다.
- `SHOP-03` 카테고리는 `푸드 / 영양 / 케어 / 패션 / 펫로스 / 라이프`를 기준으로 하고, 반려동물 필터에 `소동물`을 추가한다. `푸드·영양` 통합 여부와 slug는 결정 필요.
- `SHOP-04` 가격 필터는 `전체 / 2만원 미만 / 2-5만원 / 5-10만원 / 10만원 이상`으로 표기한다.
- `SHOP-05` 상품 카드: `SELECTED` 배지와 `잠시 품절` 문구를 삭제한다. 디렉터 추천 영역의 배지는 `DAILY PICK`으로 바꾸며 옆 설명은 삭제한다. `DAILY PICK`의 데이터 기준은 결정 필요.
- `SHOP-06` 후기 0개 상품은 별점·후기 영역을 미노출하고, 후기 등록 시 자동 노출한다.
- `SHOP-07` 고민 필터 옵션은 `전체 / 피부 / 관절 / 체중 / 구강 / 냄새`로 확인됐다. `냄새` slug는 결정 필요.
- `SHOP-08` empty-state: 제목 `선택한 조건에 맞는 상품을 찾지 못했어요.`는 유지, 보조문 `조건을 조금 넓혀 다시 살펴볼까요?`는 삭제, 버튼은 `필터 초기화`로 변경한다.
- `SHOP-09` 브랜드 필터에서 각 입점 브랜드 선택 시 해당 상품이 노출되어야 한다.
- `SHOP-10` 카테고리 필터도 등록 상품과 정상 연결되어야 한다.
- `SHOP-11` 검색창은 상품명, 브랜드명, 키워드 검색을 모두 지원해야 하며 예시로 `써니사이드업` 검색 시 등록 상품 노출이 필요하다.
- `SHOP-12` 브랜드명 표기는 한글+영문 통일 기준을 따른다.

## 케어

- `CARE-01` 케어 목록: `06 CARE` 표기는 작게, 번호보다 고민명이 먼저 보이도록 숫자 크기를 줄이고 카드 크기·줄 수를 동일하게 정렬한다.
- `CARE-02` `추가로 살펴볼 생활 케어`는 페이지 하단 안내 영역으로 이동한다.
- `CARE-03` 눈물 케어 상세: `눈물 CARE`를 `눈물 케어`로 수정하고, 제목 `눈물 자국, 닦아주는 것만으로 충분할까요?`, 본문 `매일 닦아도 반복된다면, 관리 방법부터 다시 살펴볼 필요가 있어요.`를 반영한다.
- `CARE-04` 눈물 케어 구성은 `01 원인 살펴보기`, `02 집에서 관리하기`, `03 병원 방문 판단하기`로 정렬한다.
- `CARE-05` 눈물 증상 목록에는 갈색·적갈색 자국, 눈물 양 증가, 눈 주변 털 축축함, 노란 눈곱, 눈 비빔/긁음, 한쪽 눈물 증가를 반영한다.
- `CARE-06` 병원 진료 신호에는 심한 충혈/부음, 노란색·녹색 눈곱 지속, 눈을 잘 못 뜸/찡그림, 반복적인 심한 비빔/긁음, 눈이 뿌옇게 보임, 눈 또는 눈꺼풀 상처를 반영한다.

## 브랜드 공통

- `BRAND-00` 브랜드 페이지 히어로: 영어 문구는 유지하고 제목 `우리 아이를 생각한다면, 좋은 선택이 필요합니다.`가 줄바꿈/크기 조정으로 그대로 노출되어야 한다. 본문은 `우리 아이와의 일상에 도움이 되길 바라는 마음으로, 백조오브제가 선택한 브랜드를 소개합니다.` 검증 브랜드 수는 유지한다.
- `BRAND-01` 공통 브랜드 카드 설명:
  - 노블독: 꾸준한 구강 관리를 고민하는 브랜드
  - 알로밍: 교감의 시간을 제품으로 설계하는 브랜드
  - 오미프로: 몸속의 작은 변화까지 고민하는 영양 브랜드
  - 페네핏: 더 많은 아이들이 함께할 수 있는 식탁을 고민하는 브랜드
  - 써니사이드업: 연구의 시작부터 생명을 먼저 생각하는 브랜드
  - 챠콜스토리: 숯의 가치를 반려동물에게 전하는 브랜드
  - RE:펫: 펫로스를 가장 가까이에서 경험한 작가가 만드는 브랜드
  - 메종슈슈: 입히는 대상이 아닌, 함께 살아가는 존재로 대하는 브랜드
- `BRAND-02` `WHAT WE VALUE`: `WHO / VALUE / PRINCIPLE / SAFETY / BELIEF`의 5개 기준과 고객 문구를 반영한다.
- `BRAND-03` 브랜드 설명·태그는 `10 (1).hwpx`/`10 (2).hwpx`의 통일 문구를 따른다. 알로밍 `그루밍/케어`, 오미프로 `장/뼈건강`, 페네핏 `영양/간식`, 써니사이드업 `케어/라이프`, 챠콜스토리 `탈취/위생`, RE:펫 `펫로스/오브제`, 메종슈슈 `의류/패션`, 노블독은 기존 태그 유지.
- `BRAND-04` 브랜드 페이지 통계 영역은 높이를 줄여 간결하게 구성하고 `등록 상품 수`, `카테고리 수`는 삭제 가능하다.
- `AUDIT-01` 브랜드 상세 우측 상태 문구는 `입점 자료 확인 중`이 아니라 `Audit Completed`로 정규화한다.
- `AUDIT-02` Audit 원문 또는 요약의 노출 범위는 결정 필요이며, 검증 전 완료 처리하지 않는다.

## 브랜드별

- `BRAND-NOBLEDOG`: 한글 `노블독`, 영문 `Noble Dog`, 소개 `꾸준한 구강 관리가 일상에 자리 잡을 수 있도록 돕는 브랜드`, 태그 `강아지/고양이/상품 개수`, 카테고리 `케어`, 관련 고민 `구강 · 양치`. 상세 철학과 검토 항목은 `노블독1.hwpx`, `노블독2.hwpx`, `노블독_백조오브제_AUDIT.pdf`를 따른다.
- `BRAND-ALLOMING`: 소개 `오랜 그루밍 연구를 바탕으로, 보호자가 받은 사랑에 같은 방식으로 보답할 수 있도록 돕는 브랜드`, 태그 `강아지/고양이/상품 개수`, 카테고리 `케어 · 라이프`, 관련 고민 `그루밍 · 교감`. 상세 철학과 검토 항목은 `알로밍1 (1).hwpx`, `알로밍2 (1).hwpx`, Audit PDF를 따른다.
- `BRAND-OMIPRO`: 소개 `연구가 끝난 뒤에도 스스로 확인을 멈추지 않는 영양 브랜드`, 태그 `강아지/고양이/상품 개수`, 카테고리 `푸드 · 영양`, 관련 고민 `장 · 뼈 건강`. 상세 철학과 검토 항목은 `오미프로.hwpx`, `오미프로2.hwpx`, Audit PDF를 따른다.
- `BRAND-PENEFIT`: 소개 `기호성과 영양을 함께 고민하며 선택지를 넓혀가는 브랜드`, 태그 `강아지/고양이/굿즈/상품 개수`, 카테고리 `푸드 · 영양`, 관련 고민 `편식 · 영양 관리`. 상세 철학과 검토 항목은 `페네핏1.hwpx`, `페네핏2.hwpx`, Audit PDF를 따른다.
- `BRAND-SUNNYSIDEUP`: 표기는 `써니사이드업`으로 붙여 쓰고, 소개 `세포배양 기반 연구를 바탕으로 제품과 기술에 생명을 향한 존중을 이어가는 브랜드`, 태그 `ALL ANIMALS/사람/동물실험 대체/상품 개수`, 카테고리 `케어 · 라이프`, 관련 고민 `피부 · 데일리케어`. 상세 철학과 검토 항목은 `12.hwpx`, `13.hwpx`, Audit DOCX/PDF를 따른다.
- `BRAND-CHARCOALSTORY`: 영문 표기를 회색으로 추가하고, 소개 `숯의 본질적인 가치를 아이들의 건강한 일상에 이어가는 브랜드`, 태그 `강아지/고양이/상품 개수`, 카테고리 `케어 · 라이프`, 관련 고민 `탈취 · 습기`. `챠콜스토리 배송정책*.pdf`와 팔레트파우더 상세페이지 자료는 하위 참고자료로 분리한다.
- `BRAND-REPET`: 고객 원본에는 `RE : 펫`, `RE:펫`, 폴더명 `RE펫`이 혼재한다. 최종 표기는 결정 필요이나 현재 추천은 `RE:펫` 정규화다. 소개 `그리운 아이의 모습을 정성스럽게 구현해 다시 마주할 수 있게 하는 브랜드`, 태그 `강아지/고양이/소동물/핸드메이드/상품 개수`, 카테고리 `펫로스 · 라이프`, 관련 고민 `펫로스 · 추억`.
- `BRAND-MAISONCHOUCHOU`: 소개 `입는 아이의 편안함까지 생각하며 아름다움을 완성하는 브랜드`, 태그 `강아지/핸드메이드/상품 개수`, 카테고리 `패션 · 라이프`, 관련 고민 `체형 · 착용감`. 상세 철학과 검토 항목은 `메종슈슈1.hwpx`, `메종슈슈2.hwpx`, Audit PDF를 따른다.

## B2B / 펫보험

- `B2B-01` B2B CTA: 제목 `기준이 같다면, 함께 만들어갑니다.`, 본문 `모든 프로젝트는 백조 오브제 Audit을 거친 입점 브랜드에 한해 진행합니다. 신뢰를 바탕으로 브랜드에 가장 적합한 프로젝트를 제안합니다.`, 버튼 `파트너십 문의하기`.
- `INSURANCE-01` 펫보험 CTA는 `HOME-06` 문구와 버튼을 기준으로 확인한다.

## 관리자 / 파트너 제출자료 / 참고자료

- `ADMIN-01` 파트너 유형별 제출자료를 업체 상세 화면에서 자료 유형과 파일명 목록으로 확인할 수 있어야 한다.
- `ADMIN-02` 관리자 페이지에서 제출 파일을 바로 열람 또는 다운로드할 수 있어야 하며, 추가 제출 시 기존 업체 상세 화면에 누적되어야 한다.
- `ADMIN-03` 결제 심사자료, 통신판매업 신고증, 배송정책 PDF, Audit PDF/DOCX, 상세페이지 원본 미디어는 운영·심사·제작 참고자료로 분리 보관한다.
- `MEDIA-PALETTEPOWDER` `260825_팔레트파우더 상세페이지_백조오브제 전달용` 하위 32개 미디어는 파일명 순서만 확인됐고 Preview 판독은 미완료다.
- `MEDIA-VIDEO-01` `KakaoTalk_20260826_222724948.mp4`는 영상 파일 존재만 확인됐고 내용 판독은 미완료다.

## 완료 기준

각 ID는 고객 원본, 수정 파일, 자동 테스트, 로컬 브라우저 확인, 잔여 결정사항이 기록되어야 완료다. `visual-review-needed`가 남아 있거나 자동 검증·로컬 화면 확인이 없으면 `implementation_status`를 `완료`로 바꾸지 않는다.
