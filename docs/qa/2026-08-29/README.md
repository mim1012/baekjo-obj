# 모바일 검토 방법

로컬 `main`에서 다음 명령을 실행합니다.

```powershell
npx --yes http-server docs/qa/2026-08-29 -p 4173 -a 0.0.0.0
```

PC와 휴대폰을 같은 Wi-Fi에 연결한 뒤, PC의 사설 IP를 확인해 휴대폰에서 아래 주소를 엽니다.

```text
http://<PC의-사설-IP>:4173/
```

파일을 외부에 공개하지 않는 로컬 네트워크 전용 검토용입니다.
